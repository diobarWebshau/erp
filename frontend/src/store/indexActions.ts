import {
    removeAuth, saveAuth, clearAuth, incrementCount
} from "./slicer/authSlicer.ts";
import {
    toggleTheme
} from "./slicer/themeSlicer.ts";

export {
    removeAuth,
    saveAuth,
    clearAuth,
    incrementCount,
    toggleTheme
}